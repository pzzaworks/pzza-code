### What changed in 0.2.47

- Entering fullscreen or maximizing a tile no longer spikes CPU: resize bursts are coalesced to one fit per frame and the terminal backend is only notified once at the settled size.
- Newly created sessions always start on the automatic grid layout; old explicit tile sizes no longer leak into fresh sessions.
