### What changed in 0.2.53

- The first-run guided tour now scrolls each step's target into view before highlighting it. A step whose anchor sat off-screen previously left the highlight and its tooltip outside the viewport, so the tour looked frozen; it now brings the target on screen and always shows the ring and tip.
