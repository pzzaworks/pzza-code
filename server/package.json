{
  "name": "pzzacode-server",
  "private": true,
  "version": "0.2.6",
  "type": "module",
  "description": "PzzaCode device agent - serves persistent terminal sessions, ports and saved state to the app, per device",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
