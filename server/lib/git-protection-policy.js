export const GIT_PROTECTION_INSTRUCTIONS = `## Required Git protection

Before every commit, push, or pull request, call the git_protect tool for the exact repository and operation. Proceed only when it returns approved: true for the current staged tree or branch. Run it again after changing staged files, commits, or PR text. Use git_commit and git_create_pull_request for those operations when available. Keep the installed pre-commit and pre-push hooks enabled. Never bypass protection with --no-verify, alternate hooks, allowlists, or skipped scans. Environment files, credentials, private keys, and detected secrets must never be committed or sent to a remote. If protection is unavailable or blocks an operation, stop and resolve the finding without exposing secret values.`;
